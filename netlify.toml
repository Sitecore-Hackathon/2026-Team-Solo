# Netlify config for Personalize Connect marketplace app
# Base: repo root (required for npm workspaces) | Package: marketplace

[build]
  base = "."
  command = "pnpm turbo run build --filter=marketplace"
  publish = "packages/marketplace/.next"

[build.environment]
  NODE_VERSION = "20"
